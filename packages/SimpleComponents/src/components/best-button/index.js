import SBestButton from './best-button.vue';

// Button.install = (app) => {
//   app.component(Button.name, Button);
// }

// export const SButton = Button;

// export default SButton;

export { SBestButton };
export default SBestButton;
